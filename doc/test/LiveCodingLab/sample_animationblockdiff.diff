--- /path/to/file1	2020-09-28 23:30:39.942229878 -0800
+++ /path/to/file2	2020-09-28 23:30:50.442260588 -0800
@@ -1,7 +1,8 @@
-if (animateDiff) {
+if (!animateDiff) {
-    myFunction(x);
+    someFunction(someNewParam);
     anotherFunction(y);
-    x += 1;
+    x -= 1;
+}
 while (someCondition) {
     someLogic++;
 }
@@ -9,3 +10,6 @@
 for (int i = 0; i < someCondition; i++) {
     doSomeLogic(x);
 }
+while (anotherCondition) {
+    doOtherLogic(y);
+}

